# SAG House (Next.js)

# Worker SSE/API (local house server in sag-agent worker)
SAG_WORKER_URL=http://127.0.0.1:9473

# OpenAI TTS (optional — face panel falls back to Web Speech API)
OPENAI_API_KEY=
# HOUSE_TTS_VOICE=nova
# HOUSE_TTS_MODEL=tts-1
